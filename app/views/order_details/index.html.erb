<div class="body-height">
<div class="container">
    <div class="row">
       <h1 class="1">注文履歴一覧</h1>
    </div>
    <div class="row">
		<table class="table">
			<tr>
				<th>注文日</th>
				<th>配送先</th>
				<th>注文商品</th>
				<th>支払金額</th>
				<th>ステータス</th>
				<th>注文詳細</th>
			</tr>
			<% @orders.each do |order| %>
			<tr>
				<td>
					<%= order.ordered_date %>
				</td>
				<td>
					<%= order.ordered_zip_cod %><br>
					<%= order.ordered_address %><br>
					<%= order.ordered_name %>
				</td>
				<td><%= order.products.name %></td>
				<!--total_priceとordered_postageを定義する必要あり-->
				<td><%= order.total_price %></td>
				<td><%= order.production_status %></td>	
				<td><%= link_to"表示する", order_detail_path, class: 'btn btn-danger' %></td>
			</tr>
			<% end %>
		</table>
    </div>
</div>
</div>